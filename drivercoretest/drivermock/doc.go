// Package drivermock implements a mock driver. It should be used
// only in tests.
//
// In particular, the driver implemented in this package uses mock
// images, whose status is not stored permanently. Image status,
// therefore, can vary between tests.
package drivermock
